#pragma once

#include <iostream>
#include "User.hpp"

class Server
{
    private:
        std::string _pass;
        int         _port;
    public:
        //contenedor (map, vextor, lista?) de clientes;
        //contenedor (map, vextor, lista?) de canales;

    //metodos
        //Constructor&destructor
        //getters&setters
        //initServer()
        //connectClient()
        //reciveMsg()
        //newClient()
};
